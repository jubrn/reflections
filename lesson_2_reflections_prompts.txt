it gets added to git. somehow

its a middleware between both that allows to specify certain commits to the repo

use diff

It could be helpful to have branches for backups, experimental versions aND SO ON

they show when a branch was heading a other path